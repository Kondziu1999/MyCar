
server:
  port: 8888
management:
  endpoints:
    web:
      exposure:
        include: "*"
spring:
  application:
    name: "config-server"
  cloud:
    config:
      server:
        git:
          uri: "https://github.com/Kondziu1999/MyCarConfig.git"
          skipSslValidation: true

