server:
  port: 8765

spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: "http://localhost:8888"

  datasource:
    url: "jdbc:mysql://localhost:3306/carservice?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false"
    username: "root"
    password:  "mintajzporem"
  jpa:
    hibernate:
      ddl-auto: update

jwt:
  secret: tajnykod #here your secret key to jwt token 

